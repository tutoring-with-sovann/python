# Example of the strip() string method
# Removes leading and trailing whitespace (spaces, tabs, newlines)

# Get input from user
text = input("Enter text with extra spaces: ")

# Use strip() to remove leading/trailing spaces
clean_text = text.strip()

# Print the result
print("Text without extra spaces:", clean_text)